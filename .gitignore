# Dependencies
node_modules/

# Build output
.next/
out/
dist/
build/
*.tsbuildinfo

# Environment / secrets
.env
.env.local
.env*.local

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Test output
coverage/
playwright-report/
test-results/

# Brainstorming visual companion (local only)
.superpowers/

# Isolated worktrees. Two sessions have shared this folder and moved the
# checkout under each other; a worktree is how that stops. Its contents are a
# second copy of the repo and must never be committed into the first.
.claude/worktrees/
